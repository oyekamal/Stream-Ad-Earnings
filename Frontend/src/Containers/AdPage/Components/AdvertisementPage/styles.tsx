import styled from "styled-components";
import { Layout } from "antd";

const { Content } = Layout;

export const AdContainer = styled(Content)`
  position: relative;
  width: 98vw;
  height: 100vh;
`;

export const ImageContainer = styled(Content)`
  width: 100%;
  height: 100%;
`;
